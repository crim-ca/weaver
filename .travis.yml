language: python
branches:
  only:
    - develop
    - master
python:
  - "2.7"
env:
  - DB=mongodb
sudo: false 
services:
  - mongodb
install:
  - make install
script:
  - make docs
  - make test
#after_success:
# - coveralls
